{
  "name": "gangzi-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "E2E tests for 钢子出击",
  "scripts": {
    "e2e:install": "npx playwright install chromium",
    "e2e:smoke": "npx playwright test tests/smoke.spec.ts tests/console.spec.ts",
    "e2e": "npx playwright test",
    "e2e:ci": "npx playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
